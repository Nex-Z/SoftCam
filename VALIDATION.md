# SoftCam 验证记录

## 0.1.5 录制范围修复（2026-09-14）

- TypeScript / Vite 构建通过；8 项 Node 测试和 8 项 Rust 测试通过。
- 实际区域录制验证：150% DPI 下红框与裁剪范围对齐，暂停保留，停止和启动失败清除；600 × 400 视频保存成功，抽帧确认无红框。
- 回归脚本：`node scripts/recording-border-check.cjs`；可传入便携 EXE 路径，在受限 PATH 且移除外部引擎、FFmpeg 环境变量后验证便携包。
- 本次未重跑下面的历史长测及音频矩阵。

## 历史验证（2026-09-11）

验证日期：2026-09-11。主机：Windows 11 build 26200，NVIDIA GeForce RTX 4060 Ti。连接 3840 × 2160 横屏与 1440 × 2560 竖屏，界面测试覆盖两个显示器及不同 DPI。

最终原生引擎 SHA-256：`CA8D88B0537EAED2A3456CF9E560D5F7553DE68A5B2ABE27B2A8BFDC161D8DE3`。

## 已通过

| 检查 | 结果与证据 |
| --- | --- |
| 类型检查及构建 | TypeScript、Vite、Rust release 构建成功 |
| 自动化单元测试 | 5 个 Node 测试、8 个 Rust 测试通过，覆盖 DPI、越界、暂停、音频时间戳混合及文件保护 |
| 依赖审计 | `npm audit`：0 项漏洞 |
| 实际硬件编码检测 | H.264、HEVC、AV1 均能初始化 NVENC；64 × 64 小区域正确回退 libx264 |
| 媒体组合测试 | 1920 × 1080、2560 × 1440，30/60 FPS；应用声音、系统声音、无声、应用声音叠加默认麦克风全部完成，8 秒样本输出约 8.02–8.05 秒 |
| 音画同步 | 使用统一音频输出时钟的闪光/脉冲音测试，1080p 应用声音偏差 0–33 ms，1440p 应用声音加麦克风偏差约 17 ms；见 `artifacts/matrix-1789131191960/*.sync.json` |
| 应用音频隔离 | 两个独立进程同时播放 1 kHz / 2 kHz 测试音，录制目标进程后目标频率幅度约为另一频率的 50 倍，通过隔离断言 |
| 暂停/继续 | 早期 6 秒有效录制中插入 1.5 秒暂停，成片约 6.05 秒；后续浮条端到端流程通过 |
| 最小化与关闭 | 最小化自动暂停，恢复后继续；窗口缩放保持输出尺寸，关闭目标后自动保存；见 `artifacts/edges-1789131459415/result.json` |
| 异常恢复 | 从中断的 MKV 恢复为可读 MP4，临时源文件保留 |
| 协议与退出 | 非 JSON、错误版本、未知方法均返回错误且后续有效请求可继续；stdin EOF 时完成录制保存，退出压缩时取消并清除未完成输出 |
| 截图界面 | 真实截图、矩形标注、撤销/重做、PNG 导出与系统剪贴板写入通过；两屏区域选择的物理像素大小断言通过 |
| 全局快捷键 | 与另一测试进程的快捷键冲突会提示；在设置页直接按键修改快捷键后可保存 |
| 录制到压缩的完整界面流程 | 倒计时、浮条暂停/继续/停止、成片打开、生成抽样预览、压缩另存全部通过 |
| 文件保护与取消 | 拒绝覆盖源文件和已存在目标；取消压缩后不保留该任务的不完整输出 |
| 便携运行 | 便携 EXE 在只保留 Windows System32 的 PATH 下完成启动、640 × 360 实际录制及 ffprobe 读取；未使用开发机 PATH 中的工具 |
| 原生依赖检查 | PE 导入表仅含 Windows 系统 DLL，不依赖额外的 VCRUNTIME/MSVCP 安装 |

测试产物保存在本机 `artifacts`，不打入用户便携包。

## 长时间录制

最终版本长测通过：`artifacts/soak-1789131811158/result.json`。

- 有效录制 **1804.887 秒（30 分 4 秒）**，H.264 NVENC + 指定应用音频，输出 23,831,712 字节。
- 原生窗口画面为 1924 × 1084，包含窗口边缘；输出 54,146 帧。
- 118 次引擎内存采样中，工作集为 **72.04–90.34 MiB**，结束时 **73.65 MiB**；在该场景下未观察到持续增长。
- 视频流持续时长 1804.866 秒，音频流持续时长 1804.8635 秒，相差 **2.5 ms**。这是轨道时长检查，声画事件同步另由上表的闪光/脉冲音测试验证。
- `ffmpeg -v error -xerror -i <成片> -f null -` 完整解码通过，无错误。

早期管线长测在约 18 分钟出现编码管道退出；日志没有提供更具体的 FFmpeg 错误，不能将该次运行算作通过。该版本及失败产物保留在 `artifacts/soak-1789130514808`。最终版本包含音频时间戳对齐和退出等待修正，并独立通过完整长测；早期退出的具体原因未能从日志确认。

## 尚未实测的环境边界

- 未提供干净 Windows 11 虚拟机。限制 PATH 和检查导入表验证了随包工具与运行库独立性，但不能替代完整干净系统验收。
- 未物理拔插显示器或麦克风，以免中断主机上的其他工作；代码包含目标关闭/显示器消失/音频设备错误处理。
- 未在其他显卡、ARM、Windows 10、HDR、远程桌面、受保护视频或管理员窗口环境作兼容性承诺。
- 长测使用可重复的测试画面；高速游戏、4K 60 FPS 和多任务高负载仍需按目标机器做性能验证。

## 重跑

常规：`npm test`、`npm run build`、`node scripts/protocol-check.cjs`。

真实媒体：`node scripts/matrix.cjs`、`node scripts/smoke-engine.cjs`。矩阵脚本自行启动测试画面，`check-sync.cjs <视频路径>` 检测其声画偏差。

界面：`node scripts/ui-smoke.cjs`、`node scripts/ui-actions.cjs`、`node scripts/shortcut-check.cjs`。`ui-record.cjs` 需要先启动 `tests/fixture.cjs` 的测试窗口。

长测：`node scripts/soak.cjs`，默认有效录制满 1800 秒。便携包：`node scripts/portable-smoke.cjs`。

## 0.1.1 统一捕获交互（2026-09-11）

- 主窗口合并为“捕获”入口；冻结桌面顶部工具栏提供截图／录屏、窗口／区域／屏幕和录制配置。
- `node scripts/capture-workflow.cjs` 通过：取消后关闭所有遮罩、两块显示器不同缩放下裁剪尺寸正确、截图逐像素等于冻结图像（冻结后修改测试窗口内容）、PNG 剪贴板、真实窗口录制、暂停／继续／停止保存。
- `npm test`：5 项 Node + 8 项 Rust 测试全部通过；TypeScript 与生产构建通过。
- 本次仅向原生来源枚举增加窗口坐标，音视频管线未改动；没有重复执行 30 分钟长测，原版长测证据保留在上文。
- 窗口截图当前截取该显示器上的可见区域；窗口录屏跟随整个窗口。冻结多屏快照按显示器依次获取，不保证各屏同一时刻。
- 可复现结果：`artifacts/capture-workflow/result.json`。

## 0.1.2 提示、浮条与启动反馈（2026-09-11）

- 普通提示 4.5 秒后自动关闭，同类提示再次触发会重新计时。
- 录制浮条在捕获显示器的工作区顶部居中（顶部间距 16 DIP），每次录制重新定位并重置计时；准备阶段禁用暂停与停止，避免错误操作。
- 默认取消固定 3 秒等待，录制配置中可选择保留 3 秒倒计时。提前加载隐藏浮条，确认录制时立即显示准备状态，再启动原生编码管线。
- 压缩页删除右上角导入按钮，空状态仅保留中间入口；导入后在文件信息下方提供更换视频。
- `scripts/capture-workflow.cjs` 实测通过提示自动消失、单一导入入口、浮条显示位置、冻结截图双屏逐像素对比，以及真实录制、暂停／继续与保存。本机单次测试从点击到浮条可见为 73 ms，该指标不等同于编码器开始输出首帧时间。
- `npm test` 13 项通过；本次未修改 Rust 音视频管线，未重复长时间录制。

## 0.1.3 关闭窗口偏好（2026-09-11）

- 新增“设置 → 窗口行为 → 关闭窗口时”：默认退出程序，也可选择收起到托盘，立即保存并在重启后保留。
- 没有该字段的旧配置默认退出。自绘 × 和原生窗口关闭使用同一偏好；退出仍经过录制保存、压缩取消和引擎关闭流程。
- 初始化配置完成前禁用设置修改，避免默认配置覆盖尚未加载的保存位置。
- `node scripts/close-behavior.cjs` 通过：旧配置默认退出、托盘模式隐藏窗口、偏好持久化、原生关闭时保存录制，以及主进程、Electron 子进程、录制引擎和 FFmpeg 均结束（按本次启动的进程树验证）。测试恢复原有开发配置。
- `npm test` 13 项通过；生产构建通过。

## 0.1.4 设置按钮与默认下载目录（2026-09-11）

- 设置页“更改目录”和“保存快捷键”统一使用蓝色主按钮。
- 未配置输出目录时使用 Electron 获取的 Windows 系统 Downloads 路径，不拼接用户名或固定盘符；已有自定义目录保留。录屏输出、截图和压缩的默认保存位置共用此设置。
- 生产构建通过；运行验证确认本机实际默认路径为 `F:\Download`、自定义路径不被覆盖、两处设置按钮均应用蓝色样式。界面截图位于 `artifacts/settings014/settings.png`。
